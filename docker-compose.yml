version: '3'
services:
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
  
  app:
    build: .
    ports:
      - "8081:8081"
    environment:
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      - ollama
